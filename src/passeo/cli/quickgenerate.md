# Quick Generate

Supported: ✅

Usage example:

```bash
$ passeo quickgenerate 10 n 0
```

`length` [int] - The length of the password (Required argument)

`save` [bool] - Save the password to a Passeo quickgenerate txt file? y or n. (Required argument)

`bulk` [int] - Bulk? Leave 0 if no. If more than two, it will save the passwords to a Passeo quickgenerate bulk txt file. (Required argument)
